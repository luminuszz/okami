version: '3.7'

services:
  application:
    volumes:
      - .:/app
    build: 
      context: .
      dockerfile: "./docker/${DOCKERFILE}/dockerfile"
    container_name: okami
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env

    

